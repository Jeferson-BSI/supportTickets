import Container from '@core/components/base/Container/view';
import { theme } from '@core/theme/theme';

const FastestTicketsCarousel = () => {
  return (
    <Container width="100%" center gap={16} elevation={5} bg="white" borderRadius={32}></Container>
  );
};

export default FastestTicketsCarousel;
